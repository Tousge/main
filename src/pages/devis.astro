---
import BaseLayout from '../layouts/BaseLayout.astro';
import DevisForm from '../components/DevisForm.astro';

const schema = {
  "@context": "https://schema.org",
  "@type": "WebPage",
  "name": "Demande de devis - Solution MLM",
  "description": "Demandez un devis gratuit et personnalisé pour votre projet MLM. Notre équipe d'experts vous accompagne dans la création de votre système de marketing multi-niveau.",
  "url": "https://tousgether.com/devis",
  "mainEntity": {
    "@type": "Service",
    "name": "Création de système MLM",
    "provider": {
      "@type": "Organization",
      "name": "Tousgether"
    },
    "serviceType": "MLM Software Development",
    "areaServed": {
      "@type": "Country",
      "name": "France"
    }
  }
};

const trustItems = [
  {
    icon: `<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
    </svg>`,
    title: 'Données sécurisées',
    description: 'Conformité RGPD garantie'
  },
  {
    icon: `<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <circle cx="12" cy="12" r="10"></circle>
      <polyline points="12 6 12 12 16 14"></polyline>
    </svg>`,
    title: 'Réponse sous 24h',
    description: 'Par un expert MLM dédié'
  },
  {
    icon: `<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path>
      <polyline points="22 4 12 14.01 9 11.01"></polyline>
    </svg>`,
    title: 'Sans engagement',
    description: 'Devis 100% gratuit'
  },
  {
    icon: `<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <path d="M12 2L2 7l10 5 10-5-10-5z"></path>
      <path d="M2 17l10 5 10-5"></path>
      <path d="M2 12l10 5 10-5"></path>
    </svg>`,
    title: '+50 projets MLM',
    description: 'Réalisés avec succès'
  }
];
---

<BaseLayout
  title="Demande de devis MLM gratuit | Tousgether - Expert MLM"
  description="Obtenez un devis gratuit et personnalisé pour votre projet MLM. Solutions sur-mesure : binaire, unilevel, hybride. Intégration WordPress, Shopify, sur-mesure."
  schema={schema}
  currentPage="devis"
  keywords="devis MLM, devis marketing multi-niveau, création système MLM, logiciel MLM, plateforme parrainage, solution affiliation entreprise"
  breadcrumbs={[
    { name: 'Accueil', url: '/' },
    { name: 'Demande de devis', url: '/devis' }
  ]}
>
  <!-- Hero Section -->
  <section class="hero-devis">
    <div class="hero-background">
      <div class="gradient-orb orb-1"></div>
      <div class="gradient-orb orb-2"></div>
      <div class="grid-pattern"></div>
    </div>
    
    <div class="container">
      <div class="hero-content">
        <div class="hero-badge">
          <span class="badge-dot"></span>
          <span>Devis gratuit en 24h</span>
        </div>
        
        <h1 class="hero-title">
          Lancez votre projet
          <span class="gradient-text">MLM</span>
        </h1>
        
        <p class="hero-subtitle">
          Décrivez votre projet en quelques minutes et recevez un devis personnalisé. 
          Notre équipe d'experts vous accompagne de A à Z.
        </p>

        <!-- Trust Items -->
        <div class="trust-grid-hero">
          {trustItems.map(item => (
            <div class="trust-item-hero">
              <div class="trust-icon-hero" set:html={item.icon} />
              <div class="trust-text-hero">
                <span class="trust-title-hero">{item.title}</span>
                <span class="trust-desc-hero">{item.description}</span>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- Form Section -->
  <section class="form-section-page">
    <div class="container">
      <div class="form-layout">
        <!-- Sidebar -->
        <aside class="form-sidebar">
          <div class="sidebar-sticky">
            <div class="sidebar-card">
              <div class="sidebar-header">
                <div class="sidebar-icon">
                  <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"></path>
                    <polyline points="14 2 14 8 20 8"></polyline>
                    <line x1="16" y1="13" x2="8" y2="13"></line>
                    <line x1="16" y1="17" x2="8" y2="17"></line>
                  </svg>
                </div>
                <div>
                  <h3>Votre devis</h3>
                  <p>En quelques étapes</p>
                </div>
              </div>

              <!-- Progress -->
              <div class="progress-section">
                <div class="progress-bar">
                  <div class="progress-fill" id="progress-fill"></div>
                </div>
                <span class="progress-text" id="progress-text">Étape 1/7</span>
              </div>

              <!-- Steps Navigation -->
              <nav class="steps-nav" id="steps-nav">
                <a href="#step-1" class="step-nav-item active" data-step="1">
                  <span class="step-number">1</span>
                  <span class="step-label">Informations</span>
                  <span class="step-check">
                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3">
                      <polyline points="20 6 9 17 4 12"></polyline>
                    </svg>
                  </span>
                </a>
                <a href="#step-2" class="step-nav-item" data-step="2">
                  <span class="step-number">2</span>
                  <span class="step-label">Projet MLM</span>
                  <span class="step-check">
                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3">
                      <polyline points="20 6 9 17 4 12"></polyline>
                    </svg>
                  </span>
                </a>
                <a href="#step-3" class="step-nav-item" data-step="3">
                  <span class="step-number">3</span>
                  <span class="step-label">Fonctionnalités</span>
                  <span class="step-check">
                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3">
                      <polyline points="20 6 9 17 4 12"></polyline>
                    </svg>
                  </span>
                </a>
                <a href="#step-4" class="step-nav-item" data-step="4">
                  <span class="step-number">4</span>
                  <span class="step-label">Technique</span>
                  <span class="step-check">
                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3">
                      <polyline points="20 6 9 17 4 12"></polyline>
                    </svg>
                  </span>
                </a>
                <a href="#step-5" class="step-nav-item" data-step="5">
                  <span class="step-number">5</span>
                  <span class="step-label">Budget & Délais</span>
                  <span class="step-check">
                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3">
                      <polyline points="20 6 9 17 4 12"></polyline>
                    </svg>
                  </span>
                </a>
                <a href="#step-6" class="step-nav-item" data-step="6">
                  <span class="step-number">6</span>
                  <span class="step-label">Description</span>
                  <span class="step-check">
                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3">
                      <polyline points="20 6 9 17 4 12"></polyline>
                    </svg>
                  </span>
                </a>
                <a href="#step-7" class="step-nav-item" data-step="7">
                  <span class="step-number">7</span>
                  <span class="step-label">Envoi</span>
                  <span class="step-check">
                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3">
                      <polyline points="20 6 9 17 4 12"></polyline>
                    </svg>
                  </span>
                </a>
              </nav>
            </div>

            <!-- Help Card -->
            <div class="help-card">
              <div class="help-icon">💬</div>
              <h4>Besoin d'aide ?</h4>
              <p>Notre équipe est disponible pour répondre à vos questions.</p>
              <a href="https://calendar.app.google/H5c37vg2jMK8n9NTA" target="_blank" rel="noopener noreferrer" class="help-link">
                Planifier un appel
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <line x1="5" y1="12" x2="19" y2="12"></line>
                  <polyline points="12 5 19 12 12 19"></polyline>
                </svg>
              </a>
            </div>
          </div>
        </aside>

        <!-- Form -->
        <main class="form-main">
          <DevisForm />
        </main>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  /* Hero Section */
  .hero-devis {
    position: relative;
    padding: 160px 0 80px;
    overflow: hidden;
    background: linear-gradient(180deg, #fafafa 0%, white 100%);
  }

  .hero-background {
    position: absolute;
    inset: 0;
    pointer-events: none;
    overflow: hidden;
  }

  .gradient-orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(100px);
    opacity: 0.5;
  }

  .orb-1 {
    width: 600px;
    height: 600px;
    background: radial-gradient(circle, rgba(128, 59, 133, 0.3) 0%, transparent 70%);
    top: -200px;
    right: -100px;
    animation: floatOrb 20s ease-in-out infinite;
  }

  .orb-2 {
    width: 500px;
    height: 500px;
    background: radial-gradient(circle, rgba(232, 74, 138, 0.2) 0%, transparent 70%);
    bottom: -150px;
    left: -100px;
    animation: floatOrb 15s ease-in-out infinite reverse;
  }

  @keyframes floatOrb {
    0%, 100% { transform: translate(0, 0) scale(1); }
    50% { transform: translate(30px, -30px) scale(1.05); }
  }

  .grid-pattern {
    position: absolute;
    inset: 0;
    background-image: 
      linear-gradient(rgba(128, 59, 133, 0.02) 1px, transparent 1px),
      linear-gradient(90deg, rgba(128, 59, 133, 0.02) 1px, transparent 1px);
    background-size: 60px 60px;
    mask-image: radial-gradient(ellipse at center, black 20%, transparent 70%);
    -webkit-mask-image: radial-gradient(ellipse at center, black 20%, transparent 70%);
  }

  .hero-content {
    position: relative;
    max-width: 800px;
    margin: 0 auto;
    text-align: center;
    z-index: 1;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    padding: 10px 20px;
    background: rgba(255, 255, 255, 0.9);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(128, 59, 133, 0.15);
    border-radius: 100px;
    font-size: 14px;
    font-weight: 600;
    color: #803B85;
    margin-bottom: 28px;
    box-shadow: 0 4px 24px rgba(128, 59, 133, 0.1);
  }

  .badge-dot {
    width: 8px;
    height: 8px;
    background: linear-gradient(135deg, #803B85, #E84A8A);
    border-radius: 50%;
    animation: pulse 2s ease-in-out infinite;
  }

  @keyframes pulse {
    0%, 100% { transform: scale(1); opacity: 1; }
    50% { transform: scale(1.3); opacity: 0.7; }
  }

  .hero-title {
    font-size: clamp(40px, 6vw, 64px);
    font-weight: 700;
    color: #1d1d1f;
    line-height: 1.1;
    letter-spacing: -0.04em;
    margin-bottom: 24px;
  }

  .hero-subtitle {
    font-size: 19px;
    color: #6e6e73;
    line-height: 1.6;
    margin-bottom: 48px;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  /* Trust Grid in Hero */
  .trust-grid-hero {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 20px;
    max-width: 900px;
    margin: 0 auto;
  }

  .trust-item-hero {
    display: flex;
    align-items: center;
    gap: 14px;
    padding: 20px;
    background: rgba(255, 255, 255, 0.8);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.9);
    border-radius: 16px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.04);
    transition: all 0.3s ease;
  }

  .trust-item-hero:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 30px rgba(128, 59, 133, 0.1);
  }

  .trust-icon-hero {
    width: 48px;
    height: 48px;
    background: linear-gradient(135deg, rgba(128, 59, 133, 0.1) 0%, rgba(232, 74, 138, 0.1) 100%);
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #803B85;
    flex-shrink: 0;
  }

  .trust-text-hero {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .trust-title-hero {
    font-size: 15px;
    font-weight: 600;
    color: #1d1d1f;
  }

  .trust-desc-hero {
    font-size: 13px;
    color: #86868b;
  }

  /* Form Section */
  .form-section-page {
    padding: 60px 0 120px;
    background: linear-gradient(180deg, white 0%, #f5f5f7 100%);
  }

  .form-layout {
    display: grid;
    grid-template-columns: 320px 1fr;
    gap: 48px;
    align-items: start;
  }

  /* Sidebar */
  .form-sidebar {
    position: relative;
  }

  .sidebar-sticky {
    position: sticky;
    top: 100px;
    display: flex;
    flex-direction: column;
    gap: 24px;
  }

  .sidebar-card {
    background: white;
    border-radius: 24px;
    padding: 28px;
    border: 1px solid rgba(0, 0, 0, 0.06);
    box-shadow: 0 4px 24px rgba(0, 0, 0, 0.04);
  }

  .sidebar-header {
    display: flex;
    align-items: center;
    gap: 16px;
    margin-bottom: 28px;
    padding-bottom: 20px;
    border-bottom: 1px solid #e8e8ed;
  }

  .sidebar-icon {
    width: 52px;
    height: 52px;
    background: linear-gradient(135deg, #803B85 0%, #E84A8A 100%);
    border-radius: 14px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    flex-shrink: 0;
  }

  .sidebar-header h3 {
    font-size: 18px;
    font-weight: 700;
    color: #1d1d1f;
    margin: 0;
  }

  .sidebar-header p {
    font-size: 14px;
    color: #86868b;
    margin: 0;
  }

  /* Progress */
  .progress-section {
    margin-bottom: 24px;
  }

  .progress-bar {
    height: 6px;
    background: #e8e8ed;
    border-radius: 3px;
    overflow: hidden;
    margin-bottom: 10px;
  }

  .progress-fill {
    height: 100%;
    background: linear-gradient(90deg, #803B85, #E84A8A);
    border-radius: 3px;
    width: 14.28%;
    transition: width 0.5s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .progress-text {
    font-size: 13px;
    font-weight: 600;
    color: #803B85;
  }

  /* Steps Nav */
  .steps-nav {
    display: flex;
    flex-direction: column;
    gap: 6px;
  }

  .step-nav-item {
    display: flex;
    align-items: center;
    gap: 14px;
    padding: 14px 16px;
    border-radius: 12px;
    text-decoration: none;
    transition: all 0.3s ease;
    cursor: pointer;
  }

  .step-nav-item:hover {
    background: #f5f5f7;
  }

  .step-nav-item.active {
    background: linear-gradient(135deg, rgba(128, 59, 133, 0.08) 0%, rgba(232, 74, 138, 0.08) 100%);
  }

  .step-nav-item.completed {
    background: rgba(40, 200, 64, 0.08);
  }

  .step-number {
    width: 28px;
    height: 28px;
    background: #e8e8ed;
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 13px;
    font-weight: 700;
    color: #86868b;
    flex-shrink: 0;
    transition: all 0.3s ease;
  }

  .step-nav-item.active .step-number {
    background: linear-gradient(135deg, #803B85, #E84A8A);
    color: white;
  }

  .step-nav-item.completed .step-number {
    background: #28c840;
    color: white;
  }

  .step-label {
    font-size: 14px;
    font-weight: 500;
    color: #6e6e73;
    flex: 1;
    transition: color 0.3s ease;
  }

  .step-nav-item.active .step-label {
    color: #803B85;
    font-weight: 600;
  }

  .step-nav-item.completed .step-label {
    color: #28c840;
  }

  .step-check {
    width: 24px;
    height: 24px;
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    transition: opacity 0.3s ease;
    color: #28c840;
  }

  .step-nav-item.completed .step-check {
    opacity: 1;
  }

  /* Help Card */
  .help-card {
    background: linear-gradient(135deg, #1d1d1f 0%, #2d2d30 100%);
    border-radius: 20px;
    padding: 24px;
    color: white;
  }

  .help-icon {
    font-size: 28px;
    margin-bottom: 12px;
  }

  .help-card h4 {
    font-size: 17px;
    font-weight: 700;
    margin: 0 0 8px;
  }

  .help-card p {
    font-size: 14px;
    color: rgba(255, 255, 255, 0.7);
    margin: 0 0 16px;
    line-height: 1.5;
  }

  .help-link {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    font-size: 14px;
    font-weight: 600;
    color: #E84A8A;
    text-decoration: none;
    transition: all 0.3s ease;
  }

  .help-link:hover {
    gap: 12px;
  }

  .help-link svg {
    transition: transform 0.3s ease;
  }

  .help-link:hover svg {
    transform: translateX(4px);
  }

  /* Form Main */
  .form-main {
    min-width: 0;
  }

  /* Responsive */
  @media (max-width: 1100px) {
    .form-layout {
      grid-template-columns: 1fr;
    }

    .form-sidebar {
      display: none;
    }
  }

  @media (max-width: 900px) {
    .trust-grid-hero {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .hero-devis {
      padding: 140px 0 60px;
    }

    .hero-badge {
      font-size: 12px;
      padding: 8px 16px;
    }

    .form-section-page {
      padding: 40px 0 80px;
    }
  }

  @media (max-width: 640px) {
    .trust-grid-hero {
      grid-template-columns: 1fr;
      gap: 12px;
    }

    .trust-item-hero {
      padding: 16px;
    }
  }
</style>

<script>
  // Progress and navigation handling
  document.addEventListener('DOMContentLoaded', () => {
    const stepsNav = document.getElementById('steps-nav');
    const progressFill = document.getElementById('progress-fill');
    const progressText = document.getElementById('progress-text');
    const formSections = document.querySelectorAll('.form-section');

    if (!stepsNav || !progressFill || !progressText) return;

    // Observer for section visibility
    const observer = new IntersectionObserver((entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          const sectionEl = entry.target as HTMLElement;
          const sectionIndex = Array.from(formSections).indexOf(sectionEl) + 1;
          updateProgress(sectionIndex);
        }
      });
    }, { threshold: 0.3, rootMargin: '-100px 0px -50% 0px' });

    formSections.forEach((section) => observer.observe(section));

    function updateProgress(step: number) {
      const totalSteps = 7;
      const percentage = (step / totalSteps) * 100;
      
      progressFill.style.width = `${percentage}%`;
      progressText.textContent = `Étape ${step}/${totalSteps}`;

      // Update nav items
      stepsNav?.querySelectorAll('.step-nav-item').forEach((item, index) => {
        item.classList.remove('active');
        if (index + 1 < step) {
          item.classList.add('completed');
        } else if (index + 1 === step) {
          item.classList.add('active');
          item.classList.remove('completed');
        } else {
          item.classList.remove('completed');
        }
      });
    }

    // Click handlers for step navigation
    stepsNav?.querySelectorAll('.step-nav-item').forEach((item) => {
      item.addEventListener('click', (e) => {
        e.preventDefault();
        const step = parseInt((item as HTMLElement).dataset.step || '1');
        const targetSection = formSections[step - 1];
        if (targetSection) {
          targetSection.scrollIntoView({ behavior: 'smooth', block: 'start' });
        }
      });
    });
  });
</script>
