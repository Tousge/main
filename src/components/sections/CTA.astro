---
import Button from '../Button.astro';
---

<section class="section cta-section" id="contact">
  <div class="container">
    <div class="cta-box">
      <!-- Background Effects -->
      <div class="cta-bg">
        <div class="gradient-orb orb-1"></div>
        <div class="gradient-orb orb-2"></div>
        <div class="grid-pattern"></div>
      </div>

      <!-- Content -->
      <div class="cta-content">
        <div class="cta-badge">
          <span class="badge-icon">🚀</span>
          <span>Lancez votre projet MLM</span>
        </div>
        
        <h2 class="cta-title">
          Prêt à transformer votre 
          <span class="highlight">croissance</span> ?
        </h2>
        
        <p class="cta-text">
          Contactez-nous dès aujourd'hui pour discuter de votre projet et 
          découvrir comment Tousgether peut accélérer votre succès.
        </p>

        <div class="cta-buttons">
          <Button href="/devis" variant="white">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"></path>
              <polyline points="14 2 14 8 20 8"></polyline>
              <line x1="16" y1="13" x2="8" y2="13"></line>
              <line x1="16" y1="17" x2="8" y2="17"></line>
            </svg>
            Demander un devis gratuit
          </Button>
          <Button 
            href="https://calendar.app.google/H5c37vg2jMK8n9NTA" 
            variant="outline-white"
            target="_blank"
            rel="noopener noreferrer"
          >
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect>
              <line x1="16" y1="2" x2="16" y2="6"></line>
              <line x1="8" y1="2" x2="8" y2="6"></line>
              <line x1="3" y1="10" x2="21" y2="10"></line>
            </svg>
            Planifier un appel
          </Button>
        </div>

        <!-- Trust indicators -->
        <div class="cta-trust">
          <div class="trust-item">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path>
              <polyline points="22 4 12 14.01 9 11.01"></polyline>
            </svg>
            <span>Devis gratuit en 24h</span>
          </div>
          <div class="trust-item">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path>
              <polyline points="22 4 12 14.01 9 11.01"></polyline>
            </svg>
            <span>Sans engagement</span>
          </div>
          <div class="trust-item">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path>
              <polyline points="22 4 12 14.01 9 11.01"></polyline>
            </svg>
            <span>Expert dédié</span>
          </div>
        </div>
      </div>

      <!-- Decorative Elements -->
      <div class="cta-decoration decoration-1"></div>
      <div class="cta-decoration decoration-2"></div>
    </div>
  </div>
</section>

<style>
  .cta-section {
    background: #fafafa;
    padding: 80px 0 140px;
  }

  .cta-box {
    background: linear-gradient(135deg, #1d1d1f 0%, #0a0a0a 100%);
    border-radius: 32px;
    padding: 100px 60px;
    text-align: center;
    position: relative;
    overflow: hidden;
    border: 1px solid rgba(255, 255, 255, 0.06);
    box-shadow: 
      0 4px 24px rgba(0, 0, 0, 0.1),
      0 24px 80px rgba(0, 0, 0, 0.2);
  }

  /* Background Effects */
  .cta-bg {
    position: absolute;
    inset: 0;
    overflow: hidden;
    pointer-events: none;
  }

  .gradient-orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
  }

  .orb-1 {
    width: 600px;
    height: 600px;
    background: radial-gradient(circle, rgba(128, 59, 133, 0.4) 0%, transparent 70%);
    top: -200px;
    left: -100px;
    animation: floatOrb 15s ease-in-out infinite;
  }

  .orb-2 {
    width: 500px;
    height: 500px;
    background: radial-gradient(circle, rgba(232, 74, 138, 0.3) 0%, transparent 70%);
    bottom: -200px;
    right: -100px;
    animation: floatOrb 12s ease-in-out infinite reverse;
  }

  @keyframes floatOrb {
    0%, 100% { transform: translate(0, 0) scale(1); }
    50% { transform: translate(20px, -20px) scale(1.05); }
  }

  .grid-pattern {
    position: absolute;
    inset: 0;
    background-image: 
      linear-gradient(rgba(255, 255, 255, 0.02) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255, 255, 255, 0.02) 1px, transparent 1px);
    background-size: 60px 60px;
  }

  /* Content */
  .cta-content {
    position: relative;
    z-index: 2;
    max-width: 700px;
    margin: 0 auto;
  }

  .cta-badge {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    padding: 10px 20px;
    background: rgba(255, 255, 255, 0.08);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 100px;
    font-size: 14px;
    font-weight: 600;
    color: white;
    margin-bottom: 32px;
    backdrop-filter: blur(10px);
  }

  .badge-icon {
    font-size: 16px;
  }

  .cta-title {
    font-size: clamp(36px, 5vw, 56px);
    font-weight: 700;
    color: white;
    margin-bottom: 24px;
    letter-spacing: -0.03em;
    line-height: 1.1;
  }

  .cta-title .highlight {
    background: linear-gradient(135deg, #E84A8A 0%, #B855BD 50%, #803B85 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .cta-text {
    font-size: 19px;
    color: rgba(255, 255, 255, 0.7);
    max-width: 550px;
    margin: 0 auto 48px;
    line-height: 1.6;
  }

  .cta-buttons {
    display: flex;
    justify-content: center;
    gap: 16px;
    flex-wrap: wrap;
    margin-bottom: 48px;
  }

  /* Trust Indicators */
  .cta-trust {
    display: flex;
    justify-content: center;
    gap: 32px;
    flex-wrap: wrap;
  }

  .trust-item {
    display: flex;
    align-items: center;
    gap: 8px;
    color: rgba(255, 255, 255, 0.5);
    font-size: 14px;
    font-weight: 500;
  }

  .trust-item svg {
    color: #28c840;
  }

  /* Decorative Elements */
  .cta-decoration {
    position: absolute;
    border: 1px solid rgba(255, 255, 255, 0.06);
    border-radius: 50%;
    pointer-events: none;
  }

  .decoration-1 {
    width: 300px;
    height: 300px;
    top: -100px;
    right: -50px;
    animation: rotate 30s linear infinite;
  }

  .decoration-2 {
    width: 200px;
    height: 200px;
    bottom: -50px;
    left: -50px;
    animation: rotate 25s linear infinite reverse;
  }

  @keyframes rotate {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }

  /* Responsive */
  @media (max-width: 768px) {
    .cta-section {
      padding: 60px 0 100px;
    }

    .cta-box {
      padding: 60px 28px;
      border-radius: 24px;
    }

    .cta-buttons {
      flex-direction: column;
      align-items: center;
    }

    .cta-buttons :global(.btn) {
      width: 100%;
      max-width: 300px;
    }

    .cta-trust {
      flex-direction: column;
      align-items: center;
      gap: 16px;
    }

    .cta-decoration {
      display: none;
    }
  }
</style>
